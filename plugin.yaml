name: cjk_sanitizer_ru
version: "1.0.0"
description: Убирает CJK и другие чужие письменности из ответа модели. Кириллица, латиница и код не трогаются.
provides_hooks:
  - transform_llm_output
  - pre_llm_call
